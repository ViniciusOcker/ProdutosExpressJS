const con = require("./connection")
const { v4: uuidv4 } = require("uuid")

const register = (firstName, lastName, nickname, email, password, res) => {

}

const auth = (nickname, password, res) => {

}

exports.register = register
exports.auth = auth